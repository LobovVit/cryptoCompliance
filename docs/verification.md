# Проверка первого MVP

Дата: 16.09.2026.

Использованы Go 1.27.0 и после обновления SDK Go 1.27.1 из `/Users/vitaliy/Developer/SDK/Go/current`, отдельные каталоги GOCACHE. Для финального запуска явно согласован GOROOT с SDK и задан GOTOOLCHAIN=local.

## PostgreSQL, авторизация и AML — 16.09.2026

- Поднят `postgres:17-alpine`; схема применена при запуске backend.
- Вход владельца создал opaque-сессию; запрос `/api/deals` без cookie вернул 401.
- Тестовая сделка сохранена с суммой `100.00000001`, шестью проверками и actor из сессии.
- Вторая организация получила пустой список сделок; её `viewer` получил 403 на создание сделки и на `/api/users`.
- Запуск Chainalysis без API-ключа вернул 503 и сохранил `screening_runs.status=error`, SHA-256 и сообщение. Ошибка не была преобразована в `clear`.
- Контракт клиента Chainalysis проверен локальным HTTP transport: заголовок `X-API-Key`, ответы `match` и `clear`, хеш исходного JSON.
- `go test -race ./...`, `go vet ./...`, `node --check web/app.js`, `go build` — успешно.
- В браузере проверен экран входа и загрузка из PostgreSQL после входа владельца.

Живой вызов официального Chainalysis API не выполнен: `CHAINALYSIS_API_KEY` не предоставлен. Полный KYT-провайдер не подключён; реализован прямой санкционный скрининг адреса.

- `go test -race ./...`: успешно. Проверены создание досье, шесть ручных проверок, сохранение и повторное открытие хранилища, отчёт с основаниями, приоритет выявленных вопросов, конкурентная запись с конфликтом 409, валидация суммы/сети/результата, недоверие клиентскому статусу, отсутствие изменения памяти при ошибке записи, Host/Origin/Content-Type и лишний JSON.
- `go vet ./...`: успешно.
- `node --check web/app.js`: успешно.
- `go build`: успешно.
- В браузере проверены пустой реестр, форма создания, создание демонстрационной сделки и сохранение ручного результата «Выявлены вопросы»: статус сделки изменился на «Требует внимания», журнал содержит имя и основание.

В локальном хранилище оставлено одно явно обозначенное досье «ДЕМО · Тестовый контрагент». Оно не представляет реальную компанию или операцию. Автоматические проверки по внешним источникам не проводились. Печатный PDF и мобильный viewport отдельно не проверялись.

Home Local AI дважды использован для независимого обсуждения модели и рисков. Его предположения сверялись с реализацией и тестами; ответы модели не используются как юридические или AML-заключения.
